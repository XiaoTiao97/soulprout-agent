# ============================================================
# Soulprout Gateway 部署配置
#
# ⚠️  用户个人信息（微信账号、Agent 地址、访问令牌等）
#     请通过管理界面（http://localhost:8082）进行配置，
#     不要在此文件中填写，配置会自动保存到 gateway_data/ 目录。
# ============================================================

# ── 管理 Web 服务监听地址与端口 ───────────────────────────────
# 仅在需要修改默认值时取消注释
# GATEWAY_WEB_HOST=0.0.0.0
# GATEWAY_WEB_PORT=8082
