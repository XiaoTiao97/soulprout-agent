# **通用必填**
# 部署模式：private=本地私有化；saas=多租户 SaaS
DEPLOYMENT_MODE=private

# 大模型 API 配置：复制 agent/.model.json.example 为 agent/.model.json 后编辑

# 阿里云相关服务: Embedding模型/文件内容解析/邮箱登录推送/语音输入
# (必填!) 阿里云 DashScope（灵积模型服务） 用于Embedding
DASHSCOPE_API_KEY=sk-xxx

# (必填!) 阿里云 AK/SK 用于文件内容解析 + 邮件推送
ALIYUN_ACCESS_KEY_ID=xxx
ALIYUN_ACCESS_KEY_SECRET=xxx

# 智谱互联网搜索 必填 用于websearch
ZAI_KEY=xxx

# Soulprout模式AI配置
SOULPROUT_MODEL_SOURCE=deepseek
SOULPROUT_MODEL=deepseek-v4-pro

# 高级模型: 注重精度忽略速度
PRO_MODEL_SOURCE=deepseek
PRO_MODEL=deepseek-v4-pro

# 快速模型: 注重速度
FLASH_MODEL_SOURCE=deepseek
FLASH_MODEL=deepseek-v4-flash

# -----------------------------------------------------------------------

# 通用选填
# (选填)阿里云语音输入（一句话识别，可选）
# AK/SK 需 RAM 策略 AliyunNLSFullAccess
NLS_APP_KEY=xxx

# -----------------------------------------------------------------------

# SaaS部署必填
# 阿里云邮件推送 DM 服务（必填，用于邮箱验证码登录）
# 详见 https://help.aliyun.com/document_detail/29440.html
ALIYUN_DM_ENDPOINT=dm.aliyuncs.com
# 已在 DM 控制台验证过的发信邮箱（必填）
ALIYUN_DM_ACCOUNT_NAME=xxx@your-domain.com
# 发件人显示名（可选）
ALIYUN_DM_FROM_ALIAS=Soulprout
# 验证码有效期（秒，默认 600 = 10 分钟）
EMAIL_CODE_TTL_SECONDS=600
# 同一邮箱再次请求验证码的最小间隔（秒，默认 60）
EMAIL_CODE_RESEND_SECONDS=60

# SSO 开关及服务器IP
ENABLE_SSO_LOGIN=true
# 填写服务器公网IP
FRONTEND_URL=http://1.1.1.1:80


# 登录会话签名密钥（邮箱验证码 / SSO / 游客登录成功后，服务端用此密钥签发 JWT；
# 后续请求携带 token，服务端用同一密钥校验身份。与邮箱发信配置无关。）
# 本地试跑可暂用下方占位值；生产/SaaS 必须改为随机串，否则 token 可被伪造：
#   openssl rand -hex 32
#   或 python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=soulprout-local-dev-only-change-before-production
